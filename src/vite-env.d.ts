/// <reference types="vite-plugin-pwa/client" />
/// <reference types="vite/client" />

/** Versión real de build (fecha UTC + hash corto), inyectada por Vite. */
declare const __APP_VERSION__: string;
